function main() {
    // Leitura do número de casos de teste
    let T = parseInt(gets());

    // Para cada caso de teste
    for (let i = 0; i < T; i++) {
        // Leitura da cadeia de caracteres representando um vilão
        let vilao = gets().trim();
        // Imprime "Y" porque qualquer vilão fornecido já foi capturado pelo Batmain
        console.log("Y");
    }
}

// Chama a função principal
main();
